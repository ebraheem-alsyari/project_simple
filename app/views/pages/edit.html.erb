


<%= link_to("<< Back to List", {:action => 'index'}, :class => 'back-link') %>

<div class="pages edit">
  <h2>Update Page</h2>

  <%= form_for(@page) do |f| %>

<%= render(:partial => 'form', :locals => {:f => f}) %>



  <% end %>
</div>